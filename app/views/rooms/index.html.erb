<h1>Listing rooms</h1>

<table>
  <tr>
    <th>Location num</th>
    <th>Name</th>
    <th>Phone</th>
	<th>Floor</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @rooms.each do |room| %>
  <tr>
    <td><%= room.location_num %></td>
    <td><%= room.name %></td>
    <td><%= room.phone %></td>
	<td><%= room.floor %></td>
    <td><%= link_to 'Show', room %></td>
    <td><%= link_to 'Edit', edit_room_path(room) %></td>
    <td><%= link_to 'Destroy', room, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Room', new_room_path %>
